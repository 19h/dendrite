[package]
name = "dendrite-benchmarks"
description = "Reproducible hot-path performance baselines for Dendrite"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
publish = false

[lib]
path = "src/lib.rs"

[[bench]]
name = "hot_paths"
harness = false

[dev-dependencies]
bytes.workspace = true
criterion.workspace = true
dendrite-core = { path = "../core", version = "2.0.0-alpha.1" }
dendrite-metainfo = { path = "../metainfo", version = "2.0.0-alpha.1" }
dendrite-net = { path = "../net", version = "2.0.0-alpha.1" }

[lints]
workspace = true
